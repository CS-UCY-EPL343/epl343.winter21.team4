import { extendTheme } from "@chakra-ui/react";
const theme = extendTheme({
  colors: {
    brand: {
      green: "#16DABF",
      teal: "#81E6D9",
      white: "#FFFFFF",
      yellow: "#F6E05E",
    },
  },
});

export default theme;
